# Pinned for reproducibility across machines. Torch/torchvision are intentionally
# NOT pinned here: the Sage base image supplies a CUDA build, and a plain
# `pip install` pulls the right build for each contributor's platform.

# Sage plugin runtime + OpenCV/NumPy (the [vision] extra)
pywaggle[vision]==0.56.0

# YOLO detection
ultralytics==8.4.104
# Sparse-frame tracker's one-to-one assignment dependency
lap==0.5.12

# BioCLIP species identification
pybioclip==2.1.5

# Gemma 3 4B hazard assessment using the standard BF16 checkpoint through
# Transformers. Torch is supplied by the CUDA-enabled Sage Thor base image.
transformers==5.10.1
accelerate==1.14.0
huggingface-hub==1.24.0

# SMS wildlife alerts
twilio==9.10.9
python-dotenv==1.2.2
